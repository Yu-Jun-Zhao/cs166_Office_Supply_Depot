import { ADDITEM_TOCART, DELETEITEM_FROMCART } from "./types";

// Add cart item locally and in db
const addCartItem = item => dispatch => {};

// delete cart item locally and in db
const deleteCartItem = item => dispatch => {};

// Get all cart items when user goes to cart page
const getAllCartItemsFromDB = () => dispatch => {};
